{
  "name": "domotica-server",
  "version": "0.1.0",
  "description": "Receives and stores sensor data and provides an api for monitoring this data.",
  "keywords": [
    "domotica",
    "arduino",
    "api"
  ],
  "repository": "git:TODO",
  "author": "Jorn Luiten (https://github.com/publicJorn)",
  "license": "ISC",
  "scripts": {
    "check": "check-node-version --package",
    "dev"  : "npm run check && nodemon index.js",
    "start": "npm run check && cross-env NODE_ENV=production && node index.js",
    "test" : "npm run check && jest --config ./jest.json"
  },
  "dependencies": {
    "cross-env": "^4.0.0",
    "express": "^4.15.2",
    "socket.io": "^1.7.3",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "check-node-version": "^2.1.0",
    "jest": "^19.0.2",
    "mock-socket": "^6.0.4",
    "nodemon": "^1.11.0"
  },
  "engines": {
    "npm": ">=4.2",
    "node": ">=7.10"
  }
}

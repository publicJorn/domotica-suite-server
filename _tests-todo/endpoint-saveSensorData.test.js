// saveSensorData endpoint
// Should log any save attempt
// Should ignore config changes from monitor if it is not identified
// Should accept config changes from monitor if monitor is identified
// Should save config changes in DB
// Should return confirmation of save to monitor
// Should emit `sensorlist` to monitors
